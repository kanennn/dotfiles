preload = /usr/local/share/backgrounds/wall0.png
#if more than one preload is desired thenen continue to preload other backgrounds

#set the default wallpaper(s) seen on initial workspace(s) --depending on the number of monitors used
wallpaper = eDP-1,/usr/local/share/backgrounds/wall0.png

#enable splash text rendering over the wallpaper
splash = false

#fully disable ipc
# ipc = off

